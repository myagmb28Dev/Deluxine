# Frontend expects backend-driven presigned upload (R2).
# Backend should implement:
# - POST /sessions/presign
# - POST /sessions/:id/uploads/complete
# - GET /sessions/:id returning signed GET URLs for private objects
